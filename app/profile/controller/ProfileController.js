(function() {
    'use strict';

    /**
     * Get the main module (shared for Workout).
     */
    angular.module(appName)
    /**
     * Profile Controller.
     */
    .controller('ProfileController', Profile);

    Profile.$inject = [];

    function Profile() {
       

}

})();